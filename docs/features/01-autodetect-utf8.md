# Autotest UTF8 support

The application shall autodetect UTF8 support and only enable use of UTF8
character (emojis, block character, line characters) if supported.

The fallback for tool icons is text, "read", "write", "edit" with background color.

